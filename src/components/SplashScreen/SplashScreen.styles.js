import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    splash: {
        width: null,
        height: null,
        flex: 1,
        resizeMode: 'cover'
    },
});

export default styles;