/**
 * @flow
 */

import { SplashScreen, Main} from '../components';

/**
 * ROUTE variable define whole application route definition
 **/

const ROUTES = {
    splashScreen: SplashScreen,
    main: Main,
};

export default ROUTES;
